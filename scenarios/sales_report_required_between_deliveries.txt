# After a delivered DR, a sales report is required before the next submit

P create items=b1*2 -> dr1
P submit dr=$dr1
A approve dr=$dr1
A deliver dr=$dr1

P create items=b2*2 -> dr2

# This submit must fail until a sales report is submitted
P submit dr=$dr2

# Must never run because the runner stops on first error
show dr=$dr2
